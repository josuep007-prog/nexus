# Dependências da VERSÃO WEB para hospedagem (nuvem / demonstração).
# Enxuto de propósito: sem PyQt5 (desktop), sem pyautogui/keyboard (RPA) e sem
# pdfplumber/pytesseract/Pillow (OCR pesado + binário do Tesseract). O app sobe
# e demonstra o fluxo inteiro; a extração/automação degradam graciosamente.
#
# Para rodar TUDO localmente (incl. desktop, RPA e OCR), use requirements.txt.

Flask>=3.0
waitress>=3.0
python-dateutil>=2.8
requests>=2.31
