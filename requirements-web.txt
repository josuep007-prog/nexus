# Dependências da VERSÃO WEB para hospedagem (nuvem / demonstração).
#
# Inclui pdfplumber (Python puro, sem dependência de sistema) para que a
# extração de PDFs COM TEXTO funcione também na nuvem — é o que a página
# /extracao usa para calibrar.
#
# NÃO inclui pytesseract: o OCR de imagens exige o binário do Tesseract
# instalado no sistema operacional, que não existe na hospedagem padrão. Lá,
# imagem e PDF escaneado avisam "OCR indisponível" em vez de falhar calado.
#
# Para rodar TUDO localmente (incl. OCR de imagens), use requirements.txt —
# e instale o Tesseract com o idioma português.

Flask>=3.0
waitress>=3.0
python-dateutil>=2.8
requests>=2.31

# Leitura de PDF com camada de texto (traz Pillow junto)
pdfplumber>=0.10
